Raw log files are kept here from uploaded torrents (and when they specify that they have a log)
so that we have a nice easy place to reference them if it's ever needed.

Filenames are simply just $LogID.txt.